{
  "name": "quickwallet-monorepo",
  "private": true,
  "workspaces": [
    "backend",
    "frontend",
    "packages/*"
  ],
  "scripts": {
    "dev:backend": "yarn workspace quickwallet-backend dev",
    "dev:frontend": "yarn workspace quickwallet start",
    "android": "yarn workspace quickwallet android",
    "ios": "yarn workspace quickwallet ios",
    "build:backend": "yarn workspace quickwallet-backend build",
    "typecheck:backend": "yarn workspace quickwallet-backend typecheck",
    "typecheck:frontend": "yarn workspace quickwallet typecheck",
    "typecheck:rn-core": "yarn workspace @quickwallet/rn-core typecheck",
    "typecheck": "yarn typecheck:backend && yarn typecheck:frontend && yarn typecheck:rn-core",
    "lint:backend": "yarn workspace quickwallet-backend lint",
    "lint:frontend": "yarn workspace quickwallet lint",
    "lint:rn-core": "yarn workspace @quickwallet/rn-core lint",
    "lint": "yarn lint:backend && yarn lint:frontend && yarn lint:rn-core",
    "lint:fix": "yarn workspace quickwallet-backend lint:fix && yarn workspace quickwallet lint --fix",
    "test:backend": "yarn workspace quickwallet-backend test",
    "test:frontend": "yarn workspace quickwallet test",
    "test": "yarn test:backend && yarn test:frontend",
    "prepare": "husky || true"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.4.3",
    "prettier": "^3.9.6"
  },
  "lint-staged": {
    "*.{js,ts,json,md}": [
      "prettier --write"
    ]
  }
}
